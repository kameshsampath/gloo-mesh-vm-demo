#jinja2: lstrip_blocks: "True"
apiVersion: v1
kind: Service
metadata:
  name: "{{ istio_vm_app }}"
  namespace: "{{ istio_vm_namespace }}"
  labels:
    app: {{ istio_vm_app }}
spec:
  ports:
  - port: 8080
    name: http
    targetPort: 8080
  selector:
    app: "{{ istio_vm_app }}"